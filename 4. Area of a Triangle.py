#lets find the area of a triangle

# height = float(input("Enter height of the triangle: "))         #initialized a variable to take input height of triangle

# base = float(input("Enter base of the triangle: "))     #intialized a variable base

# area = (1/2) * height * base            #a new variable 'area' where area will be stored, after formula

# print(f"Area of Triangle is: {area}")